import '../../common/res/size_configs.dart';

class ComponentsSizes {
  /// Frequently Used
  static double defaultSpace = 2.5 * (SizeConfigs.heightMultiplier);
  static double spaceBtwItems = 1.72 * (SizeConfigs.heightMultiplier);
  static double spaceBtwSection = 3.4 * (SizeConfigs.heightMultiplier);
  static double horizontalPadding = 5.58 * (SizeConfigs.widthMultiplier);

  //padding and margin
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 14;
  static const double lg = 24.0;
  static const double xl = 32.0;

  //icons sizes
  static double iconXs = 2.79 * (SizeConfigs.imageSizeMultiplier);
  static double iconSm = 3.72 * (SizeConfigs.imageSizeMultiplier);
  static double iconMd = 5.58 * (SizeConfigs.imageSizeMultiplier);
  static double iconLg = 7.44 * (SizeConfigs.imageSizeMultiplier);

  //Font Size
  static double fontSizeSm = 1.50 * (SizeConfigs.textMultiplier);
  static double fontSizeMd = 1.72 * (SizeConfigs.textMultiplier);
  static double fontSizeLg = 1.93 * (SizeConfigs.textMultiplier);

  //Button Size
  static double buttonHeight = 5.36 * (SizeConfigs.heightMultiplier);
  static double buttonRadius = 1.18 * (SizeConfigs.heightMultiplier);

  //AppBar Height
  static double appBarHeight = 6 * (SizeConfigs.heightMultiplier);

  //image size
  static double imageThumbSize = 80.0;

  //Border Radius
  static double borderRadiusSm = 0.64 * (SizeConfigs.heightMultiplier);
  static double borderRadiusMd = 1.18 * (SizeConfigs.heightMultiplier);
  static double borderRadiusLg = 2.0 * (SizeConfigs.heightMultiplier);

  //product item dimension
  static double productItemWidth = 37 * (SizeConfigs.widthMultiplier);
  static double productItemHeight = 30.15 * (SizeConfigs.heightMultiplier);
  static double productItemRadius = 0.86 * (SizeConfigs.heightMultiplier);

  //input field
  static double imputFieldRadius = 0.43 * (SizeConfigs.heightMultiplier);
  static double spaceBtwInputFields = 1.5 * (SizeConfigs.heightMultiplier);

  //cart sizes
  static double cartRadiusLg = 16.0;
  static double cartRadiusMd = 12.0;
  static double cartRadiusSm = 10.0;
  static double cartRadiusXs = 6.0;
  static double cartElevation = 2.0;

  //Image carousel height
  static double imageCarouselHeight = 21.46 * (SizeConfigs.heightMultiplier);

  //Loading Indicator Size
  static double loadingIndicatorSize = 36.0;

  //Grid view spacing
  static double gridViewSpacing = 16.0;
}
